<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thoughts</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>

<body>
    <nav id="navbar">
        <ul>
            <li><a id="home-btn" href="/">Home</a></li>
            {{#if session.userid}}
            <li><a class="btn gray-btn" href="/thoughts/dashboard">Dashboard</a></li>
            <li><a class="btn red-btn" href="/logout">Sair</a></li>
            {{else}}
            <li><a class="btn gray-btn" href="/login">Entrar</a></li>
            <li><a class="btn blue-btn" href="/register">Cadastre-se</a></li>
            {{/if}}
        </ul>
    </nav>
    <div class="container">
        {{#if messages.alertMessage}}
        <div class="alertMessage">
            {{messages.alertMessage}}
        </div>
        {{/if}}

        {{#if messages.successMessage}}
        <div class="successMessage">
            {{messages.successMessage}}
        </div>
        {{/if}}
        {{{body}}}
    </div>

    <footer><span>Thoughts</span> &copy; 2024</footer>
</body>

</html>